//Notacion

// ; => delimitar el final de una linea
const b = 4;

// . => se utiliza en los objetos para acceder a los atributos

// movil.anchura

// [] => se usan para arreglos o listas
const ar = [1,2,3,4]
console.log(ar[2]);
// {} => Llaves para objetos, funciones y estruturas de control
const movil = {
    anchura: 5,
    altura: 10
}
// () => Se usa para funciones
function suma(a,b){
    //se escrube la funcion
}
